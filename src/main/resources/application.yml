spring:
  application:
    name: deliverycore-backend

  datasource:
    url: jdbc:mysql://localhost:3306/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
  address: 0.0.0.0

jwt:
  secret: deliveryCoreSecretKeyForJWTTokenGeneration2026SuperSecure32Chars
  expiration: 86400000

payment:
  webhook:
    secret: ${PAYMENT_WEBHOOK_SECRET:local-secret}